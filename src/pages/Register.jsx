import { useState } from 'react'
export default function Register({ onRegister, onNavigate }) { const [name,setName]=useState(''); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [role,setRole]=useState('patient'); const [specialization,setSpecialization]=useState(''); const [error,setError]=useState(''); const [loading,setLoading]=useState(false); const submit=async()=>{setError('');setLoading(true);try{await onRegister({name,email,password,role,...(role==='doctor'?{specialization}:{})})}catch(e){setError(e.message)}finally{setLoading(false)}}; return <main className="auth-page"><section className="auth-side"><button className="brand" onClick={()=>onNavigate('home')}><span>✚</span><strong>HealthBook</strong></button><div><p className="eyebrow">START YOUR JOURNEY</p><h1>A healthier tomorrow starts here.</h1><p>Everything you need to stay connected to your care.</p></div></section><section className="auth-form"><button className="back" onClick={()=>onNavigate('home')}>← Back to home</button><div><h2>Create your account</h2><p>Join HealthBook in just a few moments.</p><label>Full name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Alex Morgan" required /></label><label>Email address<input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="you@example.com" required /></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="Create a secure password" required /></label><label>Account type<select value={role} onChange={e=>setRole(e.target.value)}><option value="patient">Patient</option><option value="doctor">Doctor</option></select></label>{role==='doctor'&&<label>Specialization<input value={specialization} onChange={e=>setSpecialization(e.target.value)} placeholder="e.g. Cardiology" required /></label>}{error&&<p className="form-error">{error}</p>}<button className="btn btn-primary full" disabled={loading} onClick={submit}>{loading?'Creating account...':'Create account →'}</button><p className="switch">Already have an account? <button onClick={()=>onNavigate('login')}>Sign in</button></p></div></section></main> }
