import { useState } from 'react'
export default function Login({ onLogin, onNavigate }) { const [role,setRole]=useState('patient'); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [loading,setLoading]=useState(false); const submit=async()=>{setError('');setLoading(true);try{await onLogin({email,password,role})}catch(e){setError(e.message)}finally{setLoading(false)}}; return <main className="auth-page"><section className="auth-side"><button className="brand" onClick={()=>onNavigate('home')}><span>✚</span><strong>HealthBook</strong></button><div><p className="eyebrow">WELCOME BACK</p><h1>Good care begins with a good day.</h1><p>Access a clearer, calmer view of your health.</p></div></section><section className="auth-form"><button className="back" onClick={()=>onNavigate('home')}>← Back to home</button><div><h2>Sign in to HealthBook</h2><p>Welcome back. Please enter your details.</p><label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" /></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Enter your password" /></label><label>Sign in as<select value={role} onChange={e=>setRole(e.target.value)}><option value="patient">Patient</option><option value="doctor">Doctor</option><option value="admin">Administrator</option></select></label>{error&&<p className="form-error">{error}</p>}<button className="btn btn-primary full" disabled={loading} onClick={submit}>{loading?'Signing in...':'Sign in →'}</button><p className="switch">New to HealthBook? <button onClick={()=>onNavigate('register')}>Create an account</button></p></div></section></main> }
