export default function DoctorCard({ name, specialty, initials, color, rating = 0, reviewCount = 0, experience = 0, onBook, onView }) { return <article className="card doctor-card"><div className="avatar" style={{ background: color }}>{initials}</div><h3>{name}</h3><p>{specialty}</p><p className="rating">★ {rating ? rating.toFixed(1) : 'New'} <span> · {reviewCount} reviews{experience ? ` · ${experience} years` : ''}</span></p><div className="doctor-card-actions"><button className="btn btn-light" onClick={onView || (() => {})}>View profile</button><button className="btn btn-primary" onClick={onBook || (() => {})}>Book</button></div></article> }
